import { Device, Pixel8, PixelSize } from "./device";
/**
 * A 7.5inch HD (B) device implementation.
 */
export declare class Paper75HDB implements Device {
    readonly width: PixelSize;
    readonly height: PixelSize;
    initialize(): void;
    finalize(): void;
    draw(blackPixels: Pixel8[], redPixels: Pixel8[]): void;
    private displayOn;
    private deviceSleep;
    private reset;
    private command;
    private data;
    private waitUntilIdle;
}
